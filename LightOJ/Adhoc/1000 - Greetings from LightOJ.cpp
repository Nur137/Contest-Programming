#include<bits/stdc++.h>
using namespace std;

int main()
{
    int test;
    while(scanf("%d",&test)!=EOF)
    {
        int cas=1;
        while(test--)
        {
            int a,b;
            scanf("%d%d", &a,&b);
            printf("Case %d: %d\n",cas++,a+b);
        }

    }

}
